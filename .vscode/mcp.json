{
  // Comprehensive MCP Server configuration for OmniTask (Angular 21 / Firebase / GCP)
  "mcpServers": {
    // Cleaned out unused/duplicate workspace servers to avoid 100-tool limit
  },
  "inputs": [
    {
      "id": "github_token",
      "type": "promptString",
      "description": "GitHub Personal Access Token (PAT) for MCP GitHub server",
      "password": true
    },
    {
      "id": "google_maps_key",
      "type": "promptString",
      "description": "Google Maps API Key",
      "password": true
    }
  ]
}
